/**
 * products 상세 모달 훅 및 목록 아이템 타입 (타입은 _api/productsClient에서 re-export)
 */
export type { SingleItem, CombinationItem } from '../_api/productsClient'
export { useProductDetailModal } from './useProductDetailModal'
